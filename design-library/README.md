# Enveloped Design Library

This directory is the repository source of truth for curated visual systems. Each cultural or event library owns its templates, components, schemas, compatibility rules, survey mappings, approval state and provenance.

Planned records are not production assets. Runtime code must expose only records marked `APPROVED` whose file references pass validation.
