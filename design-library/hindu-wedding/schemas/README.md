# JSON Schemas

These Draft 2020-12 schemas define required record envelopes. Cross-file referential, cultural, pricing and filesystem constraints are enforced by `npm run validate:design-library` because JSON Schema alone cannot reliably validate them.
